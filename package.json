{
  "name": "landing_page",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build-test": "vite build --mode development",
    "build-staging": "vite build --mode staging",
    "build-production": "vite build --mode production",
      "deploy-staging": "vite build --mode staging && scp -r dist/* rocky@test.floating-gates.com:/shared_fs/data/landing-page",
      "deploy-production": "vite build --mode production && scp -r dist/* basic@floating-gates.com:/mnt/HC_Volume_102380839/data/landing-page"
      
  },
  "dependencies": {
    "vue": "^3.3.4",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "vite": "^6.3.5"
  }
}
